# Recipe Generator

A simple recipe generator, it generates a recipe PDF file after filling the required inputs.  
Currently in Arabic only. I plan to add localization later.  
It saves to local storage at the moment, so no DB or backend is needed.

## Tech used:
- Vite + React
- Material UI + CSS
